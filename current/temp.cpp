#include<stdio.h>
#include<iostream>

using namespace std;

int main()  {
    int t, counter, temp;
    char str[400], Q[400], S[400];
    cin>>t;
    while(t--)  {
        scanf("%s", str);
        counter = 0;
        while(str)  {
            temp = (int)str[i];
            if(temp >= 97 && temp <=122)    {
                
            }
        }
    }
    return 0;
}
